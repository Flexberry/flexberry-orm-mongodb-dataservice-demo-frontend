import { moduleFor, test } from 'ember-qunit';

moduleFor('route:new-platform-flexberry-orm-mongo-db-data-service-demo-reg-object-e', 'Unit | Route | new platform flexberry orm mongo db data service demo reg object e', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
